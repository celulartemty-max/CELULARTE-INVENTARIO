export default async function Login({ searchParams }: { searchParams: Promise<{error?:string}> }) { const {error}=await searchParams; return <main className="login"><form action="/api/auth/login" method="post" className="card loginCard"><div className="brand">CELULARTE</div><h1>Inventario</h1><p>Acceso para colaboradores autorizados.</p><label>Correo<input name="email" type="email" autoComplete="username" required /></label><label>Contraseña<input name="password" type="password" autoComplete="current-password" required /></label>{error&&<p className="error">Correo o contraseña incorrectos.</p>}<button type="submit">Entrar</button></form></main> }
