"use client";export type ProductAdminRow={id:string;name:string;status:string;colors:Array<{id:string;name:string;status:string}>};
export default function ProductAdmin({initial}:{initial:ProductAdminRow[]}){async function act(body:Record<string,unknown>){const r=await fetch('/api/products',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(!r.ok)alert(await r.text());else location.reload()}return <section className="card pad"><form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);act({action:'createProduct',name:f.get('name')})}} className="inline"><input name="name" placeholder="Nuevo producto" required/><button>Agregar producto</button></form>{initial.map(p=><article className="productRow" key={p.id}><div><b>{p.name}</b><small>{p.status}</small></div><div className="chips">{p.colors.map(c=><span key={c.id}>{c.name}</span>)}</div><form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);act({action:'createColor',productId:p.id,name:f.get('color')})}} className="inline"><input name="color" placeholder="Agregar color" required/><button>+ Color</button></form></article>)}</section>}
