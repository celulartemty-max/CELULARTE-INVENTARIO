import { requireUser,assertBranchAccess } from "@/lib/auth/authorization";import { sql } from "@/lib/db";import ReceptionEditor,{type ReceptionBox,type ReceptionMovement,type ReceptionProduct} from "./reception-editor";
export default async function ReceptionPage({params}:{params:Promise<{id:string}>}){const u=await requireUser();const {id}=await params;const ms=await sql`SELECT m.id::text,m.folio,m.branch_id::text,m.status::text,m.pos_registered_at,m.current_version,r.expected_boxes,b.name branch FROM inventory_movements m JOIN receptions r ON r.movement_id=m.id JOIN branches b ON b.id=m.branch_id WHERE m.id=${id}::uuid AND m.movement_type::text='RECEPTION'`;const m=ms[0] as unknown as ReceptionMovement|undefined;if(!m)return <p>No encontrada.</p>;await assertBranchAccess(u,String(m.branch_id));const boxRows=await sql`SELECT b.id::text,b.box_number,b.status::text,COALESCE(jsonb_agg(jsonb_build_object('id',l.id::text,'productId',l.product_id::text,'colorId',l.color_id::text,'model',l.model_reference,'quantity',l.quantity,'product',p.name,'color',c.name) ORDER BY l.created_at) FILTER(WHERE l.id IS NOT NULL),'[]'::jsonb) lines FROM reception_boxes b LEFT JOIN reception_box_lines l ON l.box_id=b.id LEFT JOIN products p ON p.id=l.product_id LEFT JOIN product_colors c ON c.id=l.color_id WHERE b.reception_id=(SELECT id FROM receptions WHERE movement_id=${id}::uuid) GROUP BY b.id ORDER BY b.box_number`;const productRows=await sql`SELECT p.id::text,p.name,COALESCE(jsonb_agg(jsonb_build_object('id',c.id::text,'name',c.name) ORDER BY c.name) FILTER(WHERE c.id IS NOT NULL),'[]'::jsonb) colors FROM products p LEFT JOIN product_colors c ON c.product_id=p.id AND c.status::text='ACTIVE' WHERE p.status::text='ACTIVE' GROUP BY p.id ORDER BY p.name`;return <ReceptionEditor movement={m} boxes={boxRows as unknown as ReceptionBox[]} products={productRows as unknown as ReceptionProduct[]} role={u.role}/>}
