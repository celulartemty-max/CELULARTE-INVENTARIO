import Link from "next/link";
import { requireUser } from "@/lib/auth/authorization";
export default async function AppLayout({children}:{children:React.ReactNode}) { const user=await requireUser(); return <div className="shell"><aside><div className="brand">CELULARTE</div><nav><Link href="/dashboard">Dashboard</Link><span>Entradas</span><span>Devoluciones</span><span>Salidas</span><span>Traspasos</span></nav><div className="profile"><b>{user.name}</b><small>{user.role}</small><form action="/api/auth/logout" method="post"><button>Salir</button></form></div></aside><main className="content">{children}</main></div> }
