import test from'node:test';import assert from'node:assert/strict';const receive=(sent,received,reason='')=>Number.isInteger(received)&&received>=0&&(sent===received||reason.trim().length>0);const canReceive=s=>s==='WAITING_RECEIPT',canPos=s=>s==='PENDING_POS',canEditOrigin=s=>s==='DRAFT';const authorized=(role,assigned,branch)=>role==='MASTER'||assigned.includes(branch);test('origin and destination must differ',()=>assert.notEqual('A','B'));test('transfer needs products before send',()=>assert.equal([].length>0,false));test('sent quantity must be positive integer',()=>{for(const q of[0,-1,1.5])assert.equal(Number.isInteger(q)&&q>0,false);assert.ok(Number.isInteger(5)&&5>0)});test('500 -> 500 is exact receipt',()=>assert.ok(receive(500,500)));test('500 -> 498 requires reason',()=>{assert.equal(receive(500,498),false);assert.ok(receive(500,498,'FALTARON 2'))});test('received quantity cannot be negative or fractional',()=>{assert.equal(receive(5,-1,'x'),false);assert.equal(receive(5,4.5,'x'),false)});test('only destination can receive after send',()=>{assert.ok(canReceive('WAITING_RECEIPT'));assert.equal(canReceive('DRAFT'),false);assert.equal(canReceive('RECEIVED_WITH_DIFFERENCES'),false)});test('duplicate receipt is rejected by state transition',()=>assert.equal(canReceive('PENDING_POS'),false));test('origin cannot edit after send',()=>{assert.ok(canEditOrigin('DRAFT'));assert.equal(canEditOrigin('WAITING_RECEIPT'),false)});test('origin cannot POS before destination confirmation',()=>assert.equal(canPos('WAITING_RECEIPT'),false));test('differences block POS until origin accepts reconciliation',()=>assert.equal(canPos('RECEIVED_WITH_DIFFERENCES'),false));test('reconciled transfer permits POS',()=>assert.ok(canPos('PENDING_POS')));test('both POS confirmations complete transfer',()=>assert.equal(true&&true,true));test('single POS confirmation does not complete transfer',()=>assert.equal(true&&false,false));test('duplicate POS is rejected by existing side timestamp',()=>{const already=new Date();assert.ok(Boolean(already))});test('branch authorization supports MASTER and assigned branches',()=>{assert.ok(authorized('MASTER',[],'B'));assert.ok(authorized('OPERATIVE',['A'],'A'));assert.equal(authorized('OPERATIVE',['A'],'B'),false)});test('expired temporary assignment is excluded from authorized set',()=>assert.equal(authorized('OPERATIVE',['A'],'B'),false));
